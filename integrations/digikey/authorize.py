"""
DEPRECATED — No longer needed.

Digikey auth now uses Client Credentials flow (client_id + client_secret).
Tokens are fetched and refreshed automatically by auth.py.

No manual authorization step is required.
"""

print("This script is no longer needed.")
print("Digikey tokens are now fetched automatically via Client Credentials.")
print("Just make sure DIGIKEY_CLIENT_ID and DIGIKEY_CLIENT_SECRET are set in .env")
